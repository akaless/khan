# 🏠 خان (Khan) — مستند جامع پروژه

> **شعار:** «خانِ شما، جای گپ‌های تیم»
> **نسخه:** 1.0 | **تاریخ:** آگوست ۲۰۲۶

---

## ۱. خلاصه پروژه

خان یک چت سازمانی سبک فارسی برای شبکه‌های محلی (LAN) است:

- **تک‌فایله:** سرور + UI + دیتابیس در یک باینری (~۱۱MB)
- **بدون وابستگی:** بدون اینترنت، بدون نصب، بدون سرور جدا
- **سبک:** اجرا با ~۳۰MB رم
- **امن:** Argon2id + AES-256-GCM + TLS
- **دوزبانه:** فارسی RTL + انگلیسی
- **پلتفرم‌ها:** ویندوز / لینوکس / مک (Intel + Silicon)

## ۲. اهداف و مخاطب

- شرکت‌های کوچک و متوسط ایرانی
- تیم‌هایی که نیاز به چت امن داخلی دارند
- سازمان‌هایی با محدودیت اینترنت یا نیاز به حریم خصوصی

## ۳. نقش‌ها (۳ سطح قابل مشاهده)

| نقش | کد | سطح | امکانات |
|-----|-----|------|---------|
| کاربر | `user` | ۱ | چت + تغییر رمز خودش |
| سوپروایزر | `sup` | ۲ | مدیریت گروه‌ها + افزودن عضو |
| ادمین | `adm` | ۳ | مدیریت کاربران + تنظیمات + لایسنس |

## ۴. سیستم لایسنس (Ed25519)

| وضعیت | حداکثر کاربران |
|--------|----------------|
| بدون لایسنس | ۲۰ |
| لایسنس معتبر | ۵۰ / ۱۰۰ / ۵۰۰ / ∞ |
| دستکاری‌شده | ۵ (جریمه) |

- امضای دیجیتال Ed25519 — ضد جعل
- کلید خصوصی فقط نزد سازنده
- ابزار: `scripts/gen-license` (`genkeys` | `sign`)

## ۵. امنیت

- **Argon2id** برای هش رمزها
- **AES-256-GCM** برای رمزنگاری پیام‌ها (data-at-rest)
- قفل ۵ دقیقه‌ای بعد از ۵ تلاش ناموفق
- توکن‌های تصادفی امن + مدیریت جلسه
- هش کردن همه رمزها — هیچ رمزی به‌صورت متن ساده ذخیره نمی‌شود

## ۶. پورت و شبکه

- پورت پیش‌فرض: **۱۷۲۷** (قابل تغییر ۱۷۰۰-۱۷۹۹)
- آدرس: IP یا DNS (قابل تنظیم از پنل)
- اتصال: `http://<IP-or-DNS>:<port>`

## ۷. پیام‌رسانی

- WebSocket برای چت زنده
- پیام‌ها با AES-256-GCM رمزنگاری می‌شوند
- ویرایش / حذف / واکنش / ایموجی
- وضعیت تایپینگ + آنلاین/آفلاین
- فایل‌ها: آپلود/دانلود (تا ۵۰MB)

## ۸. دیتابیس

- دیتاستور JSON سبک (فایل‌های atomic-write)
- جدول‌ها: users, rooms, room_members, messages, reactions, sessions, files
- بکاپ = کپی فایل
- قفل‌گذاری با sync.RWMutex

## ۹. API

۶۰+ اندپوینت:
- Auth: login/logout/me/change-password
- Users: CRUD + نقش + ریست رمز
- Rooms: CRUD + اعضا + DM
- Messages: تاریخچه + حذف
- Files: آپلود/دانلود
- Settings: info/license/network
- WebSocket: /ws

مستند کامل: [docs/api.md](docs/api.md)

## ۱۰. ساختار پروژه

```
khan/
├── code/
│   ├── cmd/server/       ← سرور + UI embed
│   ├── config/           ← تنظیمات
│   ├── internal/
│   │   ├── models/       ← مدل‌ها
│   │   ├── database/     ← دیتاستور
│   │   ├── repository/   ← ریپازیتوری‌ها
│   │   ├── service/      ← سرویس‌ها
│   │   ├── handler/      ← هندلرهای HTTP
│   │   └── ws/           ← WebSocket
│   └── scripts/gen-license/
├── dist/                 ← باینری‌ها
├── installers/           ← نصب‌گرها
├── branding/             ← لوگو
└── docs/                 ← مستندات
```

## ۱۱. بیلد

```bash
cd code
go build -o ../dist/khan.exe ./cmd/server          # ویندوز
GOOS=linux go build -o ../dist/khan-linux ./cmd/server
GOOS=darwin GOARCH=arm64 go build -o ../dist/khan-macos-arm64 ./cmd/server
```

یا `make build-all`

## ۱۲. تصمیمات ثبت‌شده

| # | تصمیم |
|---|-------|
| ۱ | نام «خان» — رأی کاربر |
| ۲ | زبان Go (به‌جای C#) |
| ۳ | دیتاستور JSON (به‌جای SQLite — محدودیت RAM کامپایل) |
| ۴ | لایسنس Ed25519 با جریمه هوشمند (تقلب = ضرر) |
| ۵ | پورت ۱۷۲۷ با تغییر فقط دو رقم آخر |
| ۶ | UI الهام از تلگرام (تیره + مدرن) |
| ۷ | بدون MongoDB/Electron/فدراسیون/Omnichannel |
| ۸ | ابزار لایسنس = CLI مستقل gen-license |
| ۹ | باینری تک‌فایله + go:embed |
| ۱۰ | PWA برای نصب روی موبایل |
