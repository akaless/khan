# 🏠 خان (Khan) — اسپک نسخه ۱

> **نسخه:** 1.0 | **وضعیت:** منتشر شده

## ۱. معرفی

خان یک چت سازمانی سبک فارسی برای شبکه‌های محلی است — یک باینری تک‌فایله که سرور، UI و دیتابیس را با هم حمل می‌کند.

## ۲. اهداف نسخه ۱

- ✅ چت زنده بین کاربران (WebSocket)
- ✅ گروه‌ها + گفتگوی خصوصی (DM)
- ✅ مدیریت کاربران توسط ادمین
- ✅ ۳ نقش: کاربر / سوپروایزر / ادمین
- ✅ لایسنس Ed25519 (۲۰ رایگان / معتبر / جریمه)
- ✅ امنیت: Argon2id + AES-256-GCM + قفل
- ✅ دوزبانه: فارسی RTL + انگلیسی
- ✅ بیلد ویندوز / لینوکس / مک
- ✅ PWA قابل نصب

## ۳. ممنوعیت‌های نسخه ۱

- ❌ MongoDB
- ❌ Electron
- ❌ فدراسیون
- ❌ Omnichannel
- ❌ ویدیوکنفرانس
- ❌ صدها پلاگین

## ۴. نقش‌ها

| نقش | امکانات |
|-----|---------|
| کاربر | چت + تغییر رمز خودش |
| سوپروایزر | مدیریت گروه‌ها + افزودن/حذف عضو |
| ادمین | مدیریت کاربران + ساخت کاربر + تنظیمات + لایسنس |

- فقط ادمین کاربر می‌سازد — **بدون ثبت‌نام خودکار**
- ادمین می‌تواند: ساخت/غیرفعال/حذف کاربر، ریست رمز، ارتقا سوپروایزر

## ۵. ظرفیت کاربران

| حالت | سقف |
|------|-----|
| بدون لایسنس | ۲۰ کاربر |
| لایسنس معتبر | مطابق فایل (۵۰/۱۰۰/۵۰۰/∞) |
| لایسنس دستکاری‌شده | ۵ کاربر (جریمه) |

- پیام پر بودن: «ظرفیت مجاز (۲۰ کاربر) تکمیل شده است»
- کاربران معلق حساب می‌شوند؛ فقط حذف کامل ظرفیت آزاد می‌کند

## ۶. لایسنس

- امضای Ed25519
- فایل: `license.key` (JSON + امضا)
- اعمال از پنل ادمین یا API
- حذف → بازگشت به ۲۰ کاربر
- کلید خصوصی فقط نزد سازنده

## ۷. امنیت

- هش رمز: Argon2id (با salt تصادفی)
- رمزنگاری پیام: AES-256-GCM
- قفل: ۵ تلاش ناموفق → ۵ دقیقه
- جلسه: توکن تصادفی ۳۲ بایتی
- تغییر رمز اجباری در اولین ورود (must_change_pwd)

## ۸. پیام‌رسانی

- WebSocket: اتصال با `?token=`
- رویدادها: message / edited / deleted / reaction / typing / presence
- تاریخچه: REST با صفحه‌بندی (limit + before)
- واکنش‌ها: 👍 ❤️ 😂 👏 + ایموجی‌پیکر

## ۹. رابط کاربری

- تم تیره الهام از تلگرام
- RTL فارسی + LTR انگلیسی
- موبایل: سایدبار کشویی
- دسکتاپ: ۳ ستونه (لیست / چت / اطلاعات)
- PWA: manifest + آیکون + نصب

## ۱۰. تنظیمات سرور

- پورت: ۱۷۲۷ (بازه ۱۷۰۰-۱۷۹۹)
- آدرس: `address_type: "ip" | "dns"` + مقدار
- از پنل ادمین قابل تغییر

## ۱۱. API خلاصه

| مسیر | توضیح |
|------|-------|
| POST /api/setup | نصب اولیه (ساخت ادمین) |
| POST /api/auth/login | ورود |
| GET /api/auth/me | اطلاعات من |
| GET/POST /api/users | مدیریت کاربران (ادمین) |
| GET/POST /api/rooms | اتاق‌ها |
| GET /api/messages/{room} | تاریخچه |
| GET/POST /api/settings/license | لایسنس |
| GET/POST /api/settings/network | شبکه |
| GET /ws | WebSocket |

مستند کامل: [docs/api.md](docs/api.md)

## ۱۲. پلتفرم‌های هدف

| پلتفرم | معماری | فایل |
|--------|--------|------|
| ویندوز 10/11 | x64 | khan.exe |
| لینوکس | x64 | khan-linux |
| مک اینتل | x86_64 | khan-macos-intel |
| مک سیلیکون | arm64 | khan-macos-arm64 |
